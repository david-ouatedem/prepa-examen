import 'package:flutter/material.dart';

class TutorsScreen extends StatelessWidget {
  const TutorsScreen({super.key});

  @override
  Widget build(content) {
    return Center(child: Text("Tutors Screen", style: TextStyle(fontSize: 24)));
  }
}
